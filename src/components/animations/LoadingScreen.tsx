/**
 * Boot animation has been completely removed per product architecture.
 * App launches directly to LandingPage with a 350ms fade-in transition.
 */
export function LoadingScreen() {
  return null;
}